import { SidebarProvider } from '@/Components/ui/sidebar'
import React from 'react'

const page = () => {
  return (
    <SidebarProvider>
      sidebar 1
    </SidebarProvider>
  )
}

export default page
